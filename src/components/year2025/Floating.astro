---
// Gently bobbing wrapper (pure CSS via the `animate-float` keyframe).
interface Props {
  delay?: number;
  class?: string;
}
const { delay = 0, class: className = "" } = Astro.props;
---
<div class:list={["animate-float", className]} style={`animation-delay: ${delay}s;`}>
  <slot />
</div>
